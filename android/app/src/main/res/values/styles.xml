<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme — dark, no title bar -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">#070912</item>
        <item name="colorPrimaryDark">#070912</item>
        <item name="colorAccent">#4CE0D2</item>
        <item name="android:windowBackground">#070912</item>
        <item name="android:statusBarColor">#070912</item>
        <item name="android:navigationBarColor">#070912</item>
    </style>

    <!-- No action bar theme used during actual gameplay -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:background">@null</item>
    </style>

    <!-- Launch/splash theme shown while app starts -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:statusBarColor">#070912</item>
        <item name="android:navigationBarColor">#070912</item>
    </style>

</resources>